const baseUrl = "https://aps-volynt-dev.azurewebsites.net/api";
const baseCononicalUrl = "https://aps-volynt-dev.azurewebsites.net";

const config = {
  baseUrl,
  baseCononicalUrl,
};
export default config;
